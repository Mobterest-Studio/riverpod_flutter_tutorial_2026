name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.5'
      SERVERPOD_PASSWORD_database: '7FnsNS9j8ET3zp8hY5sjY7BeaPOYRZgV'
      SERVERPOD_PASSWORD_redis: 'LBO5uMiVg34mGhiQ4TnSgnvWmYFHzLpE'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: shopwave_backend_server
        run: docker compose up --quiet-pull --quiet-build --build -d

      - name: Install dependencies
        working-directory: shopwave_backend_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: shopwave_backend_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: shopwave_backend_server
        run: serverpod generate

      - name: Run tests
        working-directory: shopwave_backend_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: shopwave_backend_server
        run: docker compose down -v
